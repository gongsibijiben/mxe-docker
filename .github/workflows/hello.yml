name: Hello World

on:
  push:
    branches: [main]
  workflow_dispatch:

jobs:
  hello:
    runs-on: ubuntu-latest
    steps:
      - run: echo "Hello, GitHub Actions!"
      - run: 'echo "触发事件: ${{ github.event_name }}"'
      - run: 'echo "当前分支: ${{ github.ref }}"'
      - run: 'echo "运行系统: ${{ runner.os }}"'
